# 📁 文件整理索引

> 文件整理方案、执行计划和快速查找指南

## ✅ 整理状态

**整理已完成** (2025-11-29)
- ✅ 82 个文件已移动到新目录结构
- ✅ 13 个代码文件中的 44 处路径引用已更新
- ✅ 所有 JSON 文件已集中到 `outputs/data/fundamentals/`

## 🔧 整理脚本

### 1. **scripts/organize_outputs.py** (文件移动脚本)
   - 功能：自动移动文件到新目录结构
   - 使用方法：
     ```bash
     # 预览（不实际移动）
     python scripts/organize_outputs.py --dry-run
     
     # 实际执行
     python scripts/organize_outputs.py --execute
     ```

### 2. **scripts/update_paths_after_organize.py** (路径更新脚本)
   - 功能：自动更新代码中的路径引用
   - 使用方法：
     ```bash
     # 预览（不实际修改）
     python scripts/update_paths_after_organize.py --dry-run
     
     # 实际执行
     python scripts/update_paths_after_organize.py --execute
     ```

## 📊 整理后的目录结构

```
outputs/
├── data/                    # 所有数据文件
│   ├── fundamentals/       # 基本面 JSON
│   ├── cash_flow/          # 现金流 CSV
│   ├── training/           # 训练数据
│   └── raw/                # 原始数据
│
├── feature_importance/     # 特征重要性分析
│   ├── plots/              # 图片
│   │   ├── short_term/     # 1-7年
│   │   ├── mid_term/       # 1-5年
│   │   └── long_term/      # 5-10年
│   └── data/               # 数据文件
│       ├── mid_term/
│       └── long_term/
│
├── figs/                   # 通用可视化
│   ├── general/            # 通用图片
│   └── phase2_long_cycle/ # 保持现有
│
├── metrics/                # 模型评估指标
└── results/                # 模型预测结果
```

## 🚀 快速执行流程

```bash
# 1. 查看整理计划
cat ORGANIZE_FILES.md

# 2. 预览文件移动（不实际移动）
python scripts/organize_outputs.py --dry-run

# 3. 实际移动文件
python scripts/organize_outputs.py --execute

# 4. 预览路径更新（不实际修改）
python scripts/update_paths_after_organize.py --dry-run

# 5. 实际更新代码中的路径
python scripts/update_paths_after_organize.py --execute

# 6. 测试（运行一个脚本验证）
python src/phase2_long_cycle/create_firm_vs_macro_heatmap.py
```

## 💡 关键分类原则

- **feature_importance/** = "哪些特征重要？"（图片+数据自包含）
- **figs/** = "数据什么样？预测准不准？"（通用可视化）

## 📝 相关文件位置

- 整理脚本：`scripts/organize_outputs.py`
- 路径更新脚本：`scripts/update_paths_after_organize.py`

## 📊 整理后的文件统计

- `outputs/data/fundamentals/` - 32 个 JSON 文件（所有基本面数据）
- `outputs/data/cash_flow/` - 5 个 CSV 文件
- `outputs/data/training/` - 4 个训练数据文件
- `outputs/data/raw/` - 3 个原始数据文件
- `outputs/feature_importance/plots/` - 15 个图片文件
- `outputs/feature_importance/data/` - 9 个数据文件
- `outputs/figs/general/` - 13 个通用可视化图片
- `outputs/results/` - 7 个模型预测结果文件

---

**最后更新**: 2025-11-29  
**整理状态**: ✅ 已完成

